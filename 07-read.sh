#!/bin/bash
# take user input and print it
read -p "enter the user running the script :" NAME
# value entered is assigned to variable NAME
echo "user running the script is $NAME"
# above is good to break sequence of execution run from the command prompt 